name: km_dollar_backend
description: API Backend Dart para KM$ - Sistema de controle financeiro para motoristas
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.3
  shelf_rate_limiter: ^1.0.1
  supabase: ^2.2.4  # Supabase client
  dart_jsonwebtoken: ^2.12.2  # JWT melhor para Dart
  crypto: ^3.0.3
  dotenv: ^4.0.1
  http: ^1.1.0
  logging: ^1.2.0

dev_dependencies:
  test: ^1.21.0
  lints: ^3.0.0

executables:
  km_dollar_backend: